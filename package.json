{
  "name": "get-ssr-data-module",
  "version": "1.0.17",
  "description": "Universal SSR data fetcher with pagination, filtering, and sorting",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "prepack": "npm run build",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:watchAll": "jest --watchAll",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^27.5.2",
    "jest": "^27.5.1",
    "rimraf": "^6.0.1",
    "ts-jest": "^27.1.5",
    "typescript": "^4.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "keywords": [
    "node",
    "pagination",
    "filter",
    "sort",
    "server-side",
    "server-side rendering",
    "SSR",
    "query-builder",
    "express",
    "mysql",
    "postgresql"
  ],
  "author": "Nikunj Rathod",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/NikunjRathod0707/server-side-query-builder.git"
  }
}
